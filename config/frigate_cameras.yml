# Auto-generated by BlinkBridge.
# Paste this block into your Frigate config under the top-level 'cameras:' key.
cameras:
  backdoor:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/backdoor
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  barn_attic_door:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/barn_attic_door
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  barn_backyard_left:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/barn_backyard_left
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  barn_backyard_right:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/barn_backyard_right
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  barn_driveway_left:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/barn_driveway_left
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  barn_driveway_right:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/barn_driveway_right
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  barn_garage:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/barn_garage
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  barn_upstairs:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/barn_upstairs
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  basement_hallway:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/basement_hallway
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  downstairs_garage:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/downstairs_garage
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  front_door:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/front_door
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  front_door_interior:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/front_door_interior
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  front_yard:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/front_yard
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  main_deck:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/main_deck
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  mud_room:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/mud_room
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  side_back:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/side_back
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  side_front:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/side_front
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
  walkway:
    ffmpeg:
      inputs:
        - path: rtsp://mediamtx:8554/walkway
          roles:
            - detect
            - record
    detect:
      enabled: true
      width: 1280
      height: 720
      fps: 1
